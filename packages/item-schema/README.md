# Item Pack v1

`.itempack` 是桌宠运行器的独立道具包。它只允许声明式 JSON 和 PNG/WebP 图片资源；运行器根据受限的 `behavior` 解释互动，不会执行包内脚本。

每个道具属于 `food` 或 `toy`。首版支持 `treat`、`ball`、`wand`、`hideout` 四种行为，宠物包、道具包和程序安装包可以分别定制、交付与售卖。

单张图片的宽高上限为 2048 px；每个包所有图片的声明像素总量上限为 8,388,608。运行器顺序解码已安装道具，避免一次并发解码大量素材造成卡顿。
