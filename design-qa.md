# 宠物管理界面 Design QA

## 验收范围与证据

- 设计来源：用户提供的“猫咪小屋”参考截图。
- 实现对象：桌面端宠物管理弹层，包括宠物选择、显隐、成长信息、资源包导入与窗口平台设置。
- 最终构建截图：[desktop-v03.png](docs/assets/desktop-v03.png)。
- 验收基准：真实 Vite `dist` 与 Electron 渲染结果，不以仅在源码开发态可见的效果代替。

## 参考图关键特征

参考图使用暖白、淡粉背景和高密度宠物陈列方式；宠物以四列圆角卡片网格排列，真实宠物形象居中展示。卡片间距均匀，选中项使用明显的蓝色粗描边与右上角蓝点，列表可纵向滚动，整体感觉柔和、轻量、像一间宠物小屋。

实现保留了这些视觉特征，但没有复制参考图中的宠物资产。所有缩略图均从用户实际安装的 `.petpack` 动画图集中裁切静止首帧，当前内置示例使用包包和菲菲的真实资源。

## 产品化调整

参考图主要承担宠物选择功能；当前产品还需要完成完整管理闭环，因此有意保留并增强以下信息和操作：

- “宠物小屋”标题与当前互动宠物摘要；
- 宠物名称、等级、亲密度与当前状态；
- 每只宠物独立的“桌面显示”开关；
- 当前宠物、桌面陪伴中、休息中三类状态；
- “添加宠物”资源包导入入口；
- 窗口平台互动开关及隐私说明。

这些内容是功能性差异，并非对参考图的视觉偏离遗漏。

## 迭代记录

### 第一轮

真实 `dist` 截图显示管理面板仍沿用约 286px 的旧弹层宽度，没有出现预期的 680px 四列卡片布局。根因是 `v03-bootstrap.js` 在运行时动态插入 `./v03.css`，Vite 没有把该样式文件纳入构建依赖，开发源码与发布产物表现不一致。

修复方式：

- 将 `v03.css` 改为 `v03-bootstrap.js` 的静态模块导入；
- 删除运行时动态 `<link>` 注入；
- 重新构建真实 `dist`；
- 增加源码合同测试，防止样式再次脱离构建图；
- 同步从静态回退 DOM 删除已废弃的主动“边缘”按钮。

### 第二轮

基于修复后的真实 `dist` 截图复核：P0 为 0，P1 为 0。未发现阻断内测的视觉或交互问题。

## 分类验收

| 类别 | 验收结果 | 结论 |
| --- | --- | --- |
| Layout | 桌面宽度下为四列卡片网格；管理弹层最大宽度 680px；宠物列表独立滚动；头部、列表、底部设置区层级清晰。 | 通过 |
| Spacing | 卡片间距、面板内边距、标题与摘要间距一致；卡片内容和显隐控制没有拥挤或相互遮挡。 | 通过 |
| Typography | 标题、宠物名称、状态、等级与说明形成清晰字号和字重层级；长名称具备截断保护。 | 通过 |
| Colors | 延续暖白、淡粉主背景；卡片使用柔和米白；当前宠物使用高识别度蓝色 3px 描边和蓝点；休息状态适度降饱和。 | 通过 |
| Imagery | 使用实际宠物包 atlas 的 idle 首帧，居中、完整、无拉伸；没有复制参考图猫资产；缺失素材有安全占位状态。 | 通过 |
| States | 当前互动、桌面陪伴、休息状态可区分；点击休息宠物可唤醒并设为当前；显隐开关、导入中禁用态和选中态完整。 | 通过 |
| Responsive | 默认四列；较窄窗口降为三列，再降为两列；列表保持滚动，不以压缩卡片文字或宠物图像换取布局。 | 通过 |
| Accessibility | 弹层使用带标题的 `dialog`；列表和列表项具备语义；卡片提供 `aria-pressed` 和操作名称；显隐开关有独立标签；键盘焦点清晰可见。 | 通过 |

## 构建验收

- Vite 构建已包含 `v03.css`，生成的 dist CSS 可检出 680px 管理面板、四列网格与蓝色选中描边规则。
- 静态回退 HTML 与构建产物均不再包含废弃的主动边缘操作。
- 最终截图来自修复后的真实构建产物，可作为本轮视觉基线。

## 结论

宠物管理界面已达到当前内测版本验收标准。第二轮 P0/P1 均为 0，参考图的核心视觉语言得到保留，同时覆盖了实际产品所需的多宠管理、成长状态、资源导入与平台互动设置。
